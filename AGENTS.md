# Agent Instructions

Before doing any coding-agent work in this frontend project, read and follow
`../GLOBAL_AGENT_INSTRUCTIONS.md`.

If this project later adds more specific instructions, apply them together
with the global baseline. More specific project instructions override the
global baseline only when they conflict.
