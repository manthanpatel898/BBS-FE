import type { CustomDecorationChoice } from '@/lib/decoration/selection-state';

export function DecorationCustomItemEditor({ item, errors, disabled, onChange, onRemove }: { item: CustomDecorationChoice; errors?: string[]; disabled: boolean; onChange: (patch: Partial<CustomDecorationChoice>) => void; onRemove: () => void }) {
  return <article className="flex gap-3 rounded-xl border border-slate-200 p-3"><img src={item.imageUrl} alt="Custom decoration" className="h-24 w-24 shrink-0 rounded-lg bg-slate-100 object-cover" /><div className="min-w-0 flex-1 space-y-2"><input aria-label="Custom item name" disabled={disabled} value={item.name} onChange={(event) => onChange({ name: event.target.value })} placeholder="Custom item name" className="w-full rounded-lg border border-slate-300 px-3 py-2 font-semibold" /><div className="grid grid-cols-[90px_1fr] gap-2"><input aria-label="Custom quantity" type="number" min="1" disabled={disabled} value={item.quantity} onChange={(event) => onChange({ quantity: Number(event.target.value) })} className="rounded-lg border border-slate-300 px-3 py-2" /><input aria-label="Custom description" disabled={disabled} value={item.description} onChange={(event) => onChange({ description: event.target.value })} placeholder="Description (optional)" className="min-w-0 rounded-lg border border-slate-300 px-3 py-2" /></div><button type="button" disabled={disabled} onClick={onRemove} className="text-xs font-bold text-red-600">Remove</button>{errors?.map((error) => <p key={error} className="text-xs text-red-600">{error}</p>)}</div></article>;
}
